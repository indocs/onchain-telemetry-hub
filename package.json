{
  "name": "onchain-telemetry-hub",
  "version": "0.1.0",
  "private": true,
  "description": "Solidity telemetry hub for on-chain metrics with gas-aware events",
  "scripts": {
    "format": "prettier --write .",
    "build": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "ts-node scripts/deploy.ts"
  },
  "dependencies": {
    "ethers": "^5.7.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.20.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.1",
    "@types/node": "^18.16.0",
    "@types/mocha": "^10.0.0",
    "chai": "^4.3.8"
  }
}
